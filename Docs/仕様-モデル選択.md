# モデル選択 UI 仕様書

---

## 目次

- [概要](#概要)
- [レイアウト](#レイアウト)
- [モデル選択](#モデル選択)
  - [動的取得（ハードコードしない）](#動的取得ハードコードしない)
  - [「おまかせ / Default」選択肢](#おまかせ--default選択肢)
- [Thinking effort](#thinking-effort)
- [状態管理・永続化](#状態管理永続化)
- [トグルバー（閉時）の表示](#トグルバー閉時の表示)
- [実装タスク（未着手）](#実装タスク未着手)

---


## 概要

プロンプト入力フィールドの上に、**モデル**と **Thinking effort** を選択できる設定 UI を追加する。
普段は現在の選択値を 1 行で表示し、タップすると設定パネルが上に展開する「トグルバー型」を採用する。

> **システム構成・技術スタック**については [CLAUDE.md](../CLAUDE.md)、画面全体の設計については [設計-画面.md](設計-画面.md) を参照。

- **ステータス**: UI 設計確定 / 実装は未着手
- **対象画面**: チャット画面（`/:repo/chat`）の入力エリア

---

## レイアウト

入力バー（画像ボタン＋テキスト＋送信ボタン）の直上に「トグルバー」を常時表示し、その上に「設定パネル」が展開する。

```
       ┌────────────────────────────────────────────────┐
       │  モデル / Model                                   │  ← 設定パネル
       │   ● おまかせ / Default（前回: Opus 4.8）          │     (トグルで開閉)
       │      CLIの既定モデルに任せる                       │
       │   ○ Claude Opus 4.8                              │
       │      最も高性能。複雑なコーディング向け             │
       │   ○ Claude Sonnet 4.6                            │
       │      バランス型。日常的なコーディング向け           │
       │   ○ Claude Haiku 4.5                             │
       │      高速・軽量。簡単なタスク向け                   │
       │   ○ Claude Fable 5      (SDK更新で自動追加)        │
       │ ──────────────────────────────────────────      │
       │  Thinking effort      ※対応モデルのみ表示          │
       │   ○ 低 / low    ● 中 / medium                    │
       │   ○ 高 / high   ○ 最大 / max                     │
       └────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────┐
│ ⚙ Sonnet 4.6 · effort: 中/medium                 ▴   │  ← トグルバー(常時表示)
├──────────────────────────────────────────────────────┤
│  [画像プレビュー行]  (画像添付時のみ)                    │
├──────────────────────────────────────────────────────┤
│ [🖼]  メッセージを入力...                   [■] [↑]    │  ← 既存の入力バー
└──────────────────────────────────────────────────────┘
```

---

## モデル選択

### 動的取得（ハードコードしない）

モデル一覧は固定で持たず、Claude Agent SDK（`@anthropic-ai/claude-agent-sdk`、現 v0.2.87）から動的に取得する。
`query()` の戻り値が持つ **`supportedModels(): Promise<ModelInfo[]>`** を利用する。

`ModelInfo` の主なフィールド:

| フィールド | 用途 |
|-----------|------|
| `value` | API 呼び出しに使うモデル ID |
| `displayName` | UI に表示する名称（そのまま表示） |
| `description` | UI に表示する説明（そのまま表示） |
| `supportsEffort` | `false` の場合、Thinking effort 行を非表示 |
| `supportedEffortLevels` | `('low' \| 'medium' \| 'high' \| 'max')[]` 対応する段階のみ表示 |
| `supportsAdaptiveThinking` | 参考情報（将来利用） |
| `supportsFastMode` | 参考情報（将来利用） |

> この方式により、SDK 更新で新モデル（例: Fable）が追加されれば、**コード改修なしに UI へ自動反映**される。

### 「おまかせ / Default」選択肢

- モデル一覧の**先頭に「おまかせ / Default」を UI 側で固定追加**する（`supportedModels()` の戻り値には含まれない）。
- **初期選択はこの「おまかせ」**とする。
- 「おまかせ」選択時は `POST /api/chat` に `model` を**送らない**（`undefined`）。SDK 仕様上、`model` 未指定なら CLI の既定モデルが使われる。
- 「おまかせ」選択時は Thinking effort 行を**非表示**とし、`effort` も送らない。

#### デフォルトの実体名の併記

Claude Code 本体と同様に「おまかせ」にデフォルトの実体名（例: `おまかせ / Default（前回: Opus 4.8）`）を併記する。
ただし **SDK には「現在のデフォルトモデルが何か」を返す API が存在しない**ため、以下の方針で実測値を取得して表示する。

- SDK が**存在しない理由（確認済み）**: `ModelInfo` に `isDefault` 等のフラグはなく、`SDKControlInitializeResponse`（`initializationResult()`）が返すのは `commands` / `agents` / `models` / `account` / `fast_mode_state` のみで、デフォルトモデルを示す値は含まれない。
- **実測手段**: Claude からの応答メッセージ（`SDKAssistantMessage.message`、Anthropic API の `BetaMessage`）に、実際に使われたモデル ID（`model` フィールド）が含まれる。「おまかせ」で送信した際の応答からこの値を拾い、`supportedModels()` の `displayName` に突き合わせて表示する。
- **表示タイミング**: 初回送信までは実体が不明なため `おまかせ / Default` のみ表示。送信後は localStorage に記録し、次回以降は `おまかせ / Default（前回: {displayName}）` と併記する。

---

## Thinking effort

- SDK の effort レベルは `EffortLevel = 'low' | 'medium' | 'high' | 'max'`。
- **選択中のモデルが対応する段階（`supportedEffortLevels`）のみ**をボタンとして表示する。
- `supportsEffort` が `false` のモデル、および「おまかせ」選択時は effort 行を非表示にする。
- ラベルは**日英併記**とする。

| value | 表示ラベル |
|-------|-----------|
| `low` | 低 / low |
| `medium` | 中 / medium |
| `high` | 高 / high |
| `max` | 最大 / max |

---

## 状態管理・永続化

- フロント state: `selectedModel`（`"default"` または `ModelInfo.value`）、`selectedEffort`（`EffortLevel | null`）。
- 選択値は **localStorage に保存**し、次回起動時に復元する（キー例: `agentnest.model` / `agentnest.effort`）。
- 復元した実モデルが現在の `supportedModels()` に存在しない場合は、**「おまかせ」へフォールバック**する。
- モデルを切り替えた際、新モデルが現在の effort に非対応なら effort を自動補正する（対応する段階の先頭、または `null`）。
- 「おまかせ」送信時に応答から得た実体モデル ID を localStorage に保存（キー例: `agentnest.defaultModelSeen`）し、次回の併記表示に使う。

---

## トグルバー（閉時）の表示

| 状態 | 表示 |
|------|------|
| 「おまかせ」選択時（実体未取得） | `⚙ おまかせ / Default` |
| 「おまかせ」選択時（実体取得済み） | `⚙ おまかせ / Default（前回: {displayName}）` |
| 実モデル選択（effort あり） | `⚙ {displayName} · effort: {日本語/英語}` |
| 実モデル選択（effort 非対応） | `⚙ {displayName}` |

---

## 実装タスク（未着手）

| 種別 | 対象 | 内容 |
|------|------|------|
| 新規 | `server/routes/` | `GET /api/models` ルート追加（`supportedModels()` を呼び `ModelInfo[]` を返す） |
| 改修 | `server/routes/chat`, `server/claude/executor.ts` | `POST /api/chat` に `model` / `effort` を受け取り、`query({ options: { model, effort } })` へ反映 |
| 新規 | `frontend/src/components/SettingsPanel.tsx` | 設定パネル（モデル/effort 選択） |
| 改修 | `frontend/src/components/MessageInput.tsx` | 入力バー上にトグルバーを追加 |
| 改修 | `frontend/src/hooks/useChat.ts` | 送信処理に `model` / `effort` を付与、localStorage 永続化 |
| 改修 | `frontend/src/hooks/useChat.ts` ほか | 「おまかせ」送信時に応答メッセージの `model`（`BetaMessage.model`）を拾い、`agentnest.defaultModelSeen` に保存して併記表示に反映 |
