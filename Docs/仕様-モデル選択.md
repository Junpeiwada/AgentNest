# モデル選択 UI 仕様書

---

## 目次

- [概要](#概要)
- [レイアウト](#レイアウト)
  - [UI 表現の方針](#ui-表現の方針)
  - [モバイル（iPhone Safari）対応](#モバイルiphone-safari対応)
- [モデル選択](#モデル選択)
  - [動的取得（ハードコードしない）](#動的取得ハードコードしない)
  - [モデル一覧の取得タイミングとキャッシュ](#モデル一覧の取得タイミングとキャッシュ)
  - [「おまかせ / Default」選択肢](#おまかせ--default選択肢)
- [Thinking effort](#thinking-effort)
  - [設計方針: effort のみを扱い、thinking の ON/OFF スイッチは設けない](#設計方針-effort-のみを扱いthinking-の-onoff-スイッチは設けない)
  - [effort レベル](#effort-レベル)
- [状態管理・永続化](#状態管理永続化)
- [トグルバー（閉時）の表示](#トグルバー閉時の表示)
- [実装タスク](#実装タスク)

---


## 概要

プロンプト入力フィールドの上に、**モデル**と **Thinking effort** を選択できる設定 UI を追加する。
普段は現在の選択値を 1 行で表示し、タップすると設定パネルが上に展開する「トグルバー型」を採用する。

> **システム構成・技術スタック**については [CLAUDE.md](../CLAUDE.md)、画面全体の設計については [設計-画面.md](設計-画面.md) を参照。

- **ステータス**: 実装済み（[実装タスク](#実装タスク)参照）
- **対象画面**: チャット画面（`/:repo/chat`）の入力エリア

---

## レイアウト

入力バー（画像ボタン＋テキスト＋送信ボタン）の直上に「トグルバー」を常時表示し、その上に「設定パネル」が展開する。

**モデルは縦リスト**（数が可変・説明併記のため）、**effort は横セグメント**（段階が少なく固定的なため）で表現する。
ラジオボタン（○●）ではなく、選択中の項目をハイライトする UI とする。

```
       ┌────────────────────────────────────────────────┐
       │  モデル / Model                                   │  ← 設定パネル
       │  ┌──────────────────────────────────────────┐   │     (トグルで開閉)
       │  │● Default (recommended)                   │   │  ← 縦リスト
       │  │   実体: Opus (1M context)                 │   │     選択中は
       │  │   CLIの既定モデルに任せる                  │   │     行ハイライト
       │  ├──────────────────────────────────────────┤   │
       │  │○ Opus (1M context)                       │   │
       │  │   最も高性能。複雑なコーディング向け        │   │
       │  ├──────────────────────────────────────────┤   │
       │  │○ Sonnet                                  │   │
       │  │   バランス型。日常的なコーディング向け      │   │
       │  ├──────────────────────────────────────────┤   │
       │  │○ Haiku / Fable …(SDK追加分は自動反映)     │   │
       │  └──────────────────────────────────────────┘   │
       │ ──────────────────────────────────────────────  │
       │  Thinking effort   ※effort対応モデルのみ表示      │
       │  ┌────┬────┬────┬─────┬─────┐                   │  ← 横セグメント
       │  │ 低 │ 中 │ 高 │最高 │最大 │                   │     選択中を
       │  │low │med │high│xhigh│ max │                   │     塗りつぶし
       │  └────┴────┴────┴─────┴─────┘                   │
       └────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────┐
│ ⚙ Default (recommended) · 実体: Opus… · effort: 中  ▴│  ← トグルバー(常時表示)
├──────────────────────────────────────────────────────┤
│  [画像プレビュー行]  (画像添付時のみ)                    │
├──────────────────────────────────────────────────────┤
│ [🖼]  メッセージを入力...                   [■] [↑]    │  ← 既存の入力バー
└──────────────────────────────────────────────────────┘
```

### UI 表現の方針

| 項目 | 表現 | 理由 |
|------|------|------|
| モデル | **縦リスト**（選択行ハイライト・説明併記） | 数が可変（Fable 等で増える）。横並びは iPhone 幅で破綻するため縦が安全 |
| Thinking effort | **横セグメントコントロール**（連結ボタン） | 段階が最大 5 個と少なく固定的。横並びが収まる |

### モバイル（iPhone Safari）対応

本プロジェクトは iPhone Safari での快適操作が要件（[仕様-AgentNest全体.md](仕様-AgentNest全体.md) 参照）。設定パネルは以下を満たす。

- パネルには**最大高さ**を設け、モデルが多い場合はパネル内を**縦スクロール**させる（入力欄やキーボードを覆い隠さない）。
- ソフトキーボード表示時は、`MessageInput` 既存の `visualViewport` 監視（[MessageInput.tsx](../frontend/src/components/MessageInput.tsx)）と整合させ、パネルがキーボードに隠れない位置に出す。
- セグメント各ボタン・モデル行のタップ領域は **44px 程度**を確保する。
- effort セグメントが iPhone 幅に収まらない場合に備え、横スクロール可（折り返さない）とする。

---

## モデル選択

### 動的取得（ハードコードしない）

モデル一覧は固定で持たず、Claude Agent SDK（`@anthropic-ai/claude-agent-sdk`、現 v0.3.220）から動的に取得する。
`query()` の戻り値が持つ **`supportedModels(): Promise<ModelInfo[]>`** を利用する（後述「[モデル一覧の取得タイミングとキャッシュ](#モデル一覧の取得タイミングとキャッシュ)」参照）。

`ModelInfo` の主なフィールド:

| フィールド | 用途 |
|-----------|------|
| `value` | API 呼び出しに使うモデル ID |
| `resolvedModel` | エイリアスが解決された実体のモデル ID（例: `default` → `claude-opus-5[1m]`）。SDK 0.3.220 で追加 |
| `displayName` | UI に表示する名称（そのまま表示） |
| `description` | UI に表示する説明（そのまま表示） |
| `supportsEffort` | `false` の場合、Thinking effort 行を非表示 |
| `supportedEffortLevels` | `('low' \| 'medium' \| 'high' \| 'xhigh' \| 'max')[]` 対応する段階のみ表示 |
| `supportsAdaptiveThinking` | 参考情報（将来利用） |
| `supportsFastMode` | 参考情報（将来利用） |

> この方式により、SDK 更新で新モデル（例: Fable）が追加されれば、**コード改修なしに UI へ自動反映**される。

### モデル一覧の取得タイミングとキャッシュ

`supportedModels()` は**トップレベル関数ではなく `query()` の戻り値（`Query` オブジェクト）のメソッド**である。
そのため一覧取得には Claude Code CLI のサブプロセス起動が伴い、**`GET /api/models` のたびに毎回起動するのはコスト過大**。以下の方針とする。

- **サーバ起動時に一度だけ取得し、メモリにキャッシュする**。`GET /api/models` はキャッシュを返すだけにする。
  - 取得経路は `query()` を起動して `supportedModels()` を呼ぶ、または `initializationResult()` の戻り値 `SDKControlInitializeResponse.models`（同じ `ModelInfo[]`）を使う。実装時にどちらか軽い方を選ぶ。
- キャッシュは `executor.ts` 側で保持し、`server/routes/` の `models` ルートから参照する（query() 呼び出しを `executor.ts` に集約する既存方針に合わせる）。
- 一覧取得に失敗した場合は、フロントは**「おまかせ」のみ**を選択肢として表示し、機能は縮退動作させる（モデル/effort なしで送信可能）。

### 「おまかせ / Default」選択肢

- SDK 0.3.220 の `supportedModels()` は**先頭に `value:"default"`（`displayName:"Default (recommended)"`）を含めて返す**。これをそのまま「おまかせ」として扱い、UI 側で固定追加はしない。
- **初期選択はこの `default`**（`DEFAULT_MODEL_VALUE`）とする。
- 「おまかせ」選択時も `POST /api/chat` に `model:"default"` を**明示送信**する（一覧の正規エントリであり SDK が受理する。特別扱いの分岐を持たない）。一覧に存在しない未知値のみサーバ側 `sanitizeModelOptions` で落とし、CLI 既定へ委譲する。
- 「おまかせ」選択時も **Thinking effort は他モデルと同様に選択・送信できる**。
  - **理由**: SDK 0.3.220 の `default` エントリは `supportsEffort: true` と全 5 段階（`supportedEffortLevels`）を返す。つまり SDK 自身が「おまかせでも effort を受け付ける」と明示しているため、特別扱いしない。
  - 旧仕様では「実体が事前に分からず effort 非対応モデルへ渡す事故を避ける」ため送らない方針だったが、`resolvedModel` で実体が判明するようになり、かつ SDK が対応段階を明示するため前提が失効した。
  - **effort 対応の判定は `supportsEffortFor()`（[modelSettings.ts](../frontend/src/lib/modelSettings.ts)）に集約する**。SDK は effort 非対応モデル（例: `haiku`）で `supportsEffort` / `supportedEffortLevels` を**フィールドごと省略**するため、`supportsEffort !== false` だけでは `undefined` を通してしまう。対応段階が実在することまで確認すること。行の表示・値の正規化・トグルバー併記のすべてが同じ判定を通る。

#### デフォルトの実体名の併記

Claude Code 本体と同様に「おまかせ」にデフォルトモデルの実体名を併記する。**SDK 0.3.220 で `ModelInfo.resolvedModel` が追加され**、`default` エントリが `claude-opus-5[1m]` のように解決済みの実体 ID を直接返すようになったため、これを使う。

- **実体名の解決**（`resolveDefaultModelLabel()` / [modelSettings.ts](../frontend/src/lib/modelSettings.ts)）
  - `default` エントリの `resolvedModel` を、**同じ `resolvedModel` を持つ別エントリの `displayName` に突合**して人間可読な名前にする（例: `claude-opus-5[1m]` → `opus[1m]` エントリの `Opus (1M context)`）。生 ID をそのまま出すより読みやすく、一覧内の他行と名前が揃う。
  - 突合時は `default` エントリ自身を**除外**する（含めると自分自身に一致して `Default (recommended)` が返り、併記の意味がなくなる）。
  - 突合先が無い場合（一覧外の新モデル等）は **`resolvedModel` の生 ID をそのまま表示**する（表示が消えるよりは ID でも出す）。
  - `resolvedModel` 自体が無い場合（古い SDK / CLI）は `null` を返し、**併記を省略**する（縮退動作）。
- **表示箇所**
  - 設定パネル: 「おまかせ」行に `実体: {実体名}` を副次行として表示（`displayName` と `description` の間）。
  - トグルバー（閉時）: `{displayName} · 実体: {実体名}`。`displayName` 自体が `Opus (1M context)` のように括弧を含むため、括弧で囲わず effort 併記と同じ**中黒区切り**に揃える。
- 「前回値」ではなく**常に現在の実体**を表示するため、設定変更後にズレる問題は起きない。

<details>
<summary>旧設計（SDK に API がなかった時代の実測方式・実装不要）</summary>

`resolvedModel` が存在しなかった頃は、**SDK には「現在のデフォルトモデルが何か」を返す API が存在しない**という前提で、以下の実測方式を想定していた。現在は不要。

- SDK が**存在しない理由（確認済み）**: `ModelInfo` に `isDefault` 等のフラグはなく、`SDKControlInitializeResponse`（`initializationResult()`）が返すのは `commands` / `agents` / `models` / `account` / `fast_mode_state` のみで、デフォルトモデルを示す値は含まれない。
- **実測手段**: Claude からの応答メッセージ（`SDKAssistantMessage.message`、Anthropic API の `BetaMessage`）に、実際に使われたモデル ID（`model` フィールド）が含まれる。「おまかせ」で送信した際の応答からこの値を拾い、`supportedModels()` の `displayName` に突き合わせて表示する。
- **メインエージェントのみ採用（誤検出防止）**: サブエージェント（Task ツール等）は別モデルで動くことがあるため、応答の `model` を無条件に採用してはならない。`SDKAssistantMessage.parent_tool_use_id === null`（＝メインエージェントの直接応答）のメッセージの `model` のみを採用する。
- **突合に失敗した場合**: 拾った `model` ID が `supportedModels()` のどの `value` にも一致しない場合（内部エイリアス・一覧外の新モデル等）は、`displayName` の代わりに**生の ID 文字列をそのまま併記**する（表示が消えるよりは ID でも出す）。
- **表示タイミング**: 初回送信までは実体が不明なため `おまかせ / Default` のみ表示。送信後は localStorage に記録し、次回以降は `おまかせ / Default（前回: {displayName}）` と併記する。あくまで「前回値」であり、設定変更後は実際の既定モデルと一致しない場合がある旨を許容する。

</details>

---

## Thinking effort

### 設計方針: effort のみを扱い、thinking の ON/OFF スイッチは設けない

SDK には thinking に関する独立した 2 つのオプションがある。

| オプション | 役割 |
|-----------|------|
| `thinking?: ThinkingConfig` | thinking の**モード**。`{type:'adaptive'}`（Claude が自動判断・新世代モデルの既定）/ `{type:'enabled', budgetTokens}`（固定トークン予算・旧モデル用）/ `{type:'disabled'}`（thinking オフ） |
| `effort?: EffortLevel` | 応答にかける**労力の強さ**。SDK 説明では「adaptive thinking と連動して thinking の深さを導く」とある |

Claude Code 本体には thinking の独立 ON/OFF スイッチがあるが、これは旧来の `thinking: enabled/disabled` 由来。
**本仕様では `thinking` オプションには触れず（SDK 既定の `adaptive` に委譲）、ユーザーには `effort` のみを選ばせる**。

- 理由: 新世代モデル（Opus 4.6+ 等）では thinking は `adaptive` が既定で、Claude が「いつ・どれだけ考えるか」を自動判断する。その深さは `effort` で誘導できるため、ON/OFF スイッチを別途設けなくても実用上十分。UI もシンプルになる。
- 将来 thinking を明示的に OFF にしたいニーズが出た場合は、effort セグメント先頭に「なし」を足して `thinking: { type: 'disabled' }` を送る拡張余地がある（現時点では未採用）。

### effort レベル

- SDK の effort レベルは `EffortLevel = 'low' | 'medium' | 'high' | 'xhigh' | 'max'`（SDK 0.3.177 で `xhigh` が追加された）。
- **選択中のモデルが対応する段階（`supportedEffortLevels`）のみ**をボタンとして表示する。
- effort 非対応のモデル（`supportsEffortFor()` が `false`＝`supportsEffort === false`、または対応段階が空／未定義）は effort 行を非表示にする。**「おまかせ」は effort 対応なので表示する**。
- ラベルは**日英併記**とする。

| value | 表示ラベル |
|-------|-----------|
| `low` | 低 / low |
| `medium` | 中 / medium |
| `high` | 高 / high |
| `xhigh` | 最高 / xhigh |
| `max` | 最大 / max |

> `xhigh` / `max` は一部の上位モデルのみ対応する（SDK 型定義のコメント参照）。本仕様は `supportedEffortLevels` から動的に段階を絞るため、対応しないモデルでは該当ボタンが出ない。レイアウト図は全 5 段階を例示しているだけで、実際の表示はモデル依存。

---

## 状態管理・永続化

- フロント state: `selectedModel`（`"default"` または `ModelInfo.value`）、`selectedEffort`（`EffortLevel | null`）。
- 選択値は **localStorage に保存**し、次回起動時に復元する（キー例: `agentnest.model` / `agentnest.effort`）。
- 復元した実モデルが現在の `supportedModels()` に存在しない場合は、**「おまかせ」へフォールバック**する。
- **effort の正規化は「モデル切替時」と「起動時の復元直後」の両方で行う**（`normalizeEffort()` の同じロジックを通す）。具体的には:
  - 選択モデルが effort 非対応（`supportsEffortFor()` が `false`）の場合 → `selectedEffort` を `null` にする。「おまかせ」は effort 対応のため対象外。
  - `selectedEffort` が選択モデルの `supportedEffortLevels` に含まれない場合 → 対応する段階の先頭、または `null` に補正する（SDK 更新で対応段階が変わるケースに対応するため、復元時にも必須）。
- **実体名の併記に永続化は不要**。`resolvedModel` が毎回の `supportedModels()` に含まれるため、都度解決すればよい（旧設計にあったリポジトリ単位キー `agentnest.defaultModelSeen.{repo}` は不要になった）。

---

## トグルバー（閉時）の表示

`{displayName}` を先頭に、該当する要素だけを**中黒（ · ）で連結**する。実体と effort は排他ではなく、両方該当すれば両方出す。

| 状態 | 表示 |
|------|------|
| 一覧未取得（縮退） | `⚙ おまかせ / Default` |
| 「おまかせ」選択時（実体解決済み＋effort あり） | `⚙ Default (recommended) · 実体: Opus (1M context) · effort: 低/low` |
| 「おまかせ」選択時（`resolvedModel` なし＝旧SDK） | `⚙ Default (recommended) · effort: 低/low` |
| 実モデル選択（effort あり） | `⚙ Sonnet · effort: 高/high` |
| 実モデル選択（effort 非対応） | `⚙ Haiku` |

- `displayName` は SDK の返り値をそのまま使う（UI 側で和名に置き換えない）。
- 実体名は「おまかせ」選択時のみ併記し、実モデル選択時は付けない（選んだモデル自身が実体のため冗長）。
- `displayName` 自体が `Opus (1M context)` のように括弧を含むため、括弧では囲わず中黒区切りに統一する。

---

## 実装タスク

| 状態 | 種別 | 対象 | 内容 |
|------|------|------|------|
| ✅ 完了 | 改修 | `server/claude/executor.ts` | `supportedModels()` を 1 回取得し**メモリキャッシュ**（成功するまでフラグを立てず、`GET /api/models` 初回アクセスで自動回復）。取得失敗時は空扱い |
| ✅ 完了 | 新規 | `server/routes/models.ts` | `GET /api/models` ルート追加（キャッシュを返す。空なら一度だけ再プリフェッチ） |
| ✅ 完了 | 改修 | `server/routes/chat`, `server/claude/executor.ts` | `POST /api/chat` で `model` / `effort` を受け取り `sanitizeModelOptions` で検証後 `query()` へ反映。「おまかせ」は一覧の正規エントリ `model:"default"` として明示送信、未知値のみ未指定でCLI既定へ委譲 |
| ✅ 完了 | 新規 | `frontend/src/components/SettingsPanel.tsx` | 設定パネル（モデル=縦リスト / effort=横セグメント）。モバイル対応（最大高さ＋スクロール、タップ領域 44px） |
| ✅ 完了 | 改修 | `frontend/src/components/MessageInput.tsx` | 入力バー上にトグルバーを追加。`visualViewport` 監視と整合させキーボードと干渉しない配置 |
| ✅ 完了 | 改修 | `frontend/src/hooks/useChat.ts` | 送信処理に `model` / `effort` を付与、localStorage 永続化、effort の正規化（切替時＋復元時） |
| ✅ 完了 | 改修 | `frontend/src/lib/modelSettings.ts`, `frontend/src/components/SettingsPanel.tsx` | **デフォルト実体名の併記**。`ModelInfo.resolvedModel`（SDK 0.3.220 で追加）を他エントリの `displayName` に突合する `resolveDefaultModelLabel()` を追加し、設定パネルの「おまかせ」行に `実体: {名前}`、トグルバーに `· 実体: {名前}` を表示。突合先が無ければ生 ID、`resolvedModel` 自体が無ければ併記省略。詳細は [デフォルトの実体名の併記](#デフォルトの実体名の併記) 節 |
| ✅ 完了 | 改修 | `frontend/src/lib/modelSettings.ts`, `frontend/src/components/SettingsPanel.tsx` | **effort 対応判定の集約**。`supportsEffortFor()` を追加し、行の表示・`normalizeEffort()`・トグルバー併記が同一判定を通るようにした。SDK が effort 非対応モデル（`haiku`）で `supportsEffort` をフィールドごと省略するため、`!== false` だけでは `undefined` を通してしまう問題に対応 |
| ✅ 完了 | 新規 | `tests/model-settings.check.ts` | `resolveDefaultModelLabel` / `buildToggleBarLabel` の単体チェック（実測相当フィクスチャ＋境界条件17ケース）。`npx tsx tests/model-settings.check.ts` で実行 |
