BASE_PROJECT_DIR=/path/to/your/projects

# Claude Code CLI の実行ファイルパス（任意）。
# 未設定なら PATH や公式インストーラの既知パス（~/.local/bin/claude 等）から自動探索する。
# 自動探索で見つからない場合・別の場所のCLIを使いたい場合のみ指定する。
# CLAUDE_CLI_PATH=/Users/you/.local/bin/claude
